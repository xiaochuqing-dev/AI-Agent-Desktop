下一 Agent 提示词

你接手 AI-Agent-Desktop 仓库。开始前核对 main、最新 GitHub Actions、reports/SIX_LINK_OBSERVABILITY_LIVE_E2E_AND_USER_VALIDATION_REPORT.md、reports/MINIMAL_GUI_ONBOARDING_AND_WINDOWS_DISTRIBUTION_REPORT.md、05_NEXT_PHASE.md、integrations/cc-connect/manifests/artifact-lock.json、architecture/control-plane-v1/13_TELEGRAM_THREE_BOT_AND_NATIVE_CONFIGURATION.md 和 contracts/control-plane-v1。

当前 Git 事实

公开 `main` 起点为 33652568727b6bb4b41ae84b99a1e2332eea6bce。当前工作区可能包含未提交的 GUI/Telegram onboarding 变更；先读取 `git status`，不要覆盖其他人的改动，不要 reset、force push、删除历史、创建分支或 PR。

产品范围固定

产品仍是面向 Windows 开发者的 Telegram AI 编程团队安装、配置、管理、诊断与恢复中心。首发仅 Windows 10/11 与 Telegram；Agent 固定为 Hermes、Claude Code、Codex；cc-connect 是 V1 核心桥梁；CC Switch 是推荐但非强制入口。不得扩展 Agent、Channel 或通用平台能力。

已交付事实

Control Plane 已有 loopback + Bearer、7 个只读 Adapter、Readiness、DryRun、Diagnostic、持久化 OperationExecutor/SSE 和 Alembic。cc-connect Windows amd64 锁定基线为 fc315d213b49d62e9d90ea4a510189d4115e636f + 既有 Patchset 0.1 + Go 1.26.5，产物 SHA256 为 cd1b0787709c0401a42f7c3ce5321184889adbfbf3b080190fee180afc977eec。本阶段不得升级上游或增加 Patch。

Windows CredentialBackend 已真实落到 keyring.backends.Windows.WinVaultKeyring，支持 put、replace、status、resolve_for_operation、delete、metadata 和 revision，不允许明文文件回退。公开 CredentialRef 固定为 telegram/hermes-bot-token、telegram/claude-bot-token、telegram/codex-bot-token。

TelegramBotApiClient、三个固定 Bot getMe 唯一身份、Webhook 显式处理、Update Lease、一次性 HMAC bind code、offset 防重放、同一 User/Group 的三私聊/三群聊 3/3 绑定已实现并通过 Fake Telegram 合成验收。六 LinkState、一次性 E2E 计划、消息关联、Session 隔离探针、代理策略、Alembic 和旧 Windows 验收向导也已交付。

当前 GUI 事实

control-plane/control_plane/gui/ 已实现 PySide6 6.7.2 + Qt Widgets + QSS 的 `0.4.0-prebeta` GUI：Welcome、固定四步 Wizard Shell、Token、私聊激活、同群检测、Completion、Dashboard、Diagnostics、Hermes readiness/conflict、QR dialog、Telegram tg:///HTTPS fallback、SVG IconRegistry、Design Tokens、统一 TitleBar/GlassDialog、Demo/HTTP/Embedded Control Plane 客户端。requirements-gui.lock 固定 PySide6 6.7.2、qrcode 8.2、Pillow 11.3.0。

GUI 通过 /api/v1/onboarding/snapshot、/api/v1/dashboard/snapshot、/api/v1/telegram/client-availability 以及现有 credentials、telegram verify、binding、poll API 工作。GUI 不读取 Telegram tdata、登录状态、群列表、聊天正文或上游私有配置。Demo 模式只用于截图和自动化测试，标题栏必须显示预览状态，Demo 结果不算真实证据。

当前证据与硬状态

新 GUI 私聊激活和群自动检测：PENDING USER LIVE VALIDATION。必须由用户在真实 Telegram 重新走新 GUI 并明确确认后才能改变。

Windows 10 x64：PENDING WINDOWS 10 VALIDATION。Windows 11 的既有 Control Plane/cc-connect 合成证据不能替代 Windows 10。

MSI、正式安装器/卸载器、Start Menu/快捷方式发布、代码签名和证书：DEFERRED。不要把旧 `control-plane/dist/AI-Agent-Desktop-stage-a-*` Tk Validation-Wizard 包描述成新 GUI candidate。

历史边界

2026-08-07 用户直接在旧入口 Telegram 验证 Hermes、Claude Code、Codex 私聊/群聊六链路并确认通过；这可保持历史 LIVE_VERIFIED，但没有新 GUI 入口、三次结构化 getMe、3/3 binding 或 correlation 持久化记录。不得伪报 Hermes 实时消息、新 GUI live、真实 Token 或新 GUI Windows 打包已验证。

Hermes 已有配置保持 external-first，不由 Control Plane 静默接管 Provider、Model、Tool 或生命周期；对已安装但 Telegram 未配置的 Hermes，Native Adapter 仅通过官方公开 `.env` 与 Gateway CLI 完成 allowlisted Telegram 配置和 Gateway 生命周期，并要求冲突显式确认、失败可回滚。锁定上游 management API 监听所有网卡、原生 Group Chat 白名单和 deep health 保持 partial/unsupported，不为此增加 Patch。

当前本地验证

当前工作区全量 pytest 为 264 passed、2 skipped、1 warning；Ruff、format、`mypy control_plane` 112 files、契约验证和 `0.4.0-prebeta` candidate validator 均通过。新 GUI candidate 已在 Windows 11 x64 构建；远端 CI 仍以最新 GitHub Actions run 为准。scripts 目录额外 mypy 不计入 CI 门禁。

下一步允许

下一步请用户真实走私聊/群自动检测，最后在 Windows 10 x64 普通用户环境验收。app_icon PNG/ICO 透明 alpha 与黑底边缘、Windows 11 candidate、--version、--headless、offscreen smoke、PE GUI subsystem、Qt 资源、manifest、SHA256、Secret 扫描和无真实 Telegram 访问检查均已完成。

如要形成机器可审计 Telegram live 证据，必须用户显式发起新三 Bot getMe、同一 User/Group 的 3/3 binding 和六链路 correlation；Fake、Demo、旧日志摘要或用户口头结论都不能代替。

禁止事项

不要升级 cc-connect 或增加 Patch；不要修改 Reference Baseline、计划任务、Watchdog、PATH、注册表、junction、外部配置、真实 Bot/Provider 配置；不要静默 deleteWebhook 或接管 external 生命周期；不要安装/升级 Hermes；不要发送默认真实测试消息；不要创建通用 Bot/Vault/进程平台、Provider 编辑器、新 Channel、MSI 或签名流程。

文档与保护文件

所有报告、日志、Operation、SSE、SQLite、配置、候选包和 GUI 快照继续禁止 Secret、绑定码明文和消息正文。不要修改 reference-baseline/、PUBLIC_FILE_MANIFEST.txt 或 SHA256SUMS.txt；若公开文件集合发生变化，交由发布负责人单独重新生成并复核。
